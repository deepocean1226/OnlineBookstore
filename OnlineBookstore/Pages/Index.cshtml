@page 
@model IndexModel
@{
    ViewData["Title"] = "主页";
}

    <div class="text-center">
        <h1 class="display-4">主页</h1>
        <p>这是我的主页</p>
        @if (Model.User_Now!=null)
        {
            <p>此时用户为-@Model.User_Now.UserName</p>
        }
    </div>
